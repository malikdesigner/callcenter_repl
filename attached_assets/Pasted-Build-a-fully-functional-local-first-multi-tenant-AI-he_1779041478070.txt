Build a fully functional local-first multi-tenant AI healthcare appointment SaaS platform optimized specifically for my existing Windows PC environment using Windows as the primary runtime and WSL2 Ubuntu only for OpenClaw orchestration and Linux-based AI services where necessary. Do not use Docker, Kubernetes, cloud services, or any heavy container orchestration. The entire system must run directly on my local machine using native Windows services, local Python environments, local Node.js processes, local PostgreSQL or SQLite, local Redis optional fallback memory mode, local ChromaDB persistent storage, local Ollama models, and the already-installed OpenClaw instance inside WSL. Use my existing hardware efficiently: approximately 16GB RAM and 8GB NVIDIA GPU. Optimize aggressively for low VRAM usage, low RAM usage, stable long-running execution, fast startup, low latency streaming responses, and prevention of memory leaks or GPU crashes. Use Python 3.12 with FastAPI async architecture for the backend, SQLAlchemy ORM, Alembic-ready migrations, JWT authentication with refresh tokens, modular repository-service-controller architecture, structured logging, health checks, rate limiting, validation, and clean production-style engineering practices while remaining lightweight for local execution. Use SQLite by default for development with architecture designed for future PostgreSQL migration without changing business logic. Implement strict hospital-level multi-tenancy using hospital_id filtering across all APIs, database queries, Redis memory keys, Chroma collections, OpenClaw sessions, and AI tool executions. Use the already-running OpenClaw instance in WSL as the AI orchestration and tool-calling runtime connected to my existing Ollama installation running on Windows. The LLM must never directly access databases or business logic; instead implement secure FastAPI tool APIs including get_available_doctors(), check_available_slots(), create_appointment(), cancel_appointment(), reschedule_appointment(), search_hospital_knowledge(), route_symptoms_to_specialty(), and get_hospital_timings(). Use lightweight quantized Ollama models such as qwen2.5:7b-instruct-q4_K_M or llama3.1:8b-instruct-q4 optimized for my GPU. Responses must stream token-by-token in real time using SSE or WebSockets and support Urdu, Roman Urdu, and English conversational interactions. Implement medically safe conversational logic where the assistant never diagnoses diseases but safely routes symptoms to specialties such as cardiology, dermatology, gastroenterology, pulmonology, ophthalmology, or general medicine. Build multilingual RAG using lightweight embedding models like multilingual-e5-small or bge-small and store embeddings locally in ChromaDB persistent mode with strict hospital isolation. The system must answer hospital-specific queries such as MRI availability, insurance acceptance, female doctor availability, timings, services, and policies using semantic retrieval before LLM generation. Build transactional appointment booking with doctor schedules, recurring slots, blocked timings, holidays, doctor leaves, emergency closures, Redis or in-memory temporary slot locking, and concurrency-safe booking logic independent of the LLM. Build three frontend applications using Next.js 15, React, TypeScript, TailwindCSS, and ShadCN UI running natively on Windows without Docker: a hospital admin dashboard, patient chat portal, and browser voice assistant interface with Urdu RTL support, mobile responsiveness, real-time streaming AI responses, appointment flows, analytics, and voice controls. Implement browser voice conversations using faster-whisper small or medium models with GPU acceleration for low-latency transcription and Piper Urdu TTS for realistic local voice synthesis with streaming playback. Use OpenClaw inside WSL only as the orchestration runtime while all databases, Node.js frontends, FastAPI backend, Ollama, ChromaDB, SQLite/PostgreSQL, and supporting services run directly on Windows for maximum performance and simpler debugging. Automatically configure environment variables, local setup scripts, startup scripts, folder structure, APIs, frontend pages, database schema, OpenClaw integrations, authentication flows, hospital personas, RAG pipelines, voice services, and testing infrastructure. Ensure the final architecture remains modular, scalable, production-grade, and cloud-migration-ready while being fully optimized for stable, real-time execution entirely on my local Windows machine with WSL-assisted OpenClaw integration only.